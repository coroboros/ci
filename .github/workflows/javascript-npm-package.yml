# Pipeline: check-docs -> setup-npmrc -> check-lint -> build-js -> build-version (rules) -> test-unit -> deploy-package (tag) -> notify.
# Pull requests are excluded by the top-level on: block of each reusable workflow.
name: JavaScript NPM Package CI

on:
  workflow_call:
    inputs:
      node-version:
        type: string
        required: false
        default: "22"
      release-branch-pattern:
        type: string
        required: false
        default: '^release[/]((0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*))$'
      release-source-commit-pattern:
        type: string
        required: false
        default: "^Merge branch 'release[/]((0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*))' into 'main'"
      npm-extra-config:
        type: string
        required: false
        default: ""
      provenance:
        description: When `true`, pass `--provenance` to `pnpm publish` (requires npm Trusted Publisher OIDC). Set to `false` when only `NPM_PACKAGE_REGISTRY_TOKEN` is available.
        type: boolean
        required: false
        default: true
    secrets:
      NPM_CONFIG_FILE:
        required: true
      NPM_PACKAGE_REGISTRY:
        required: true
      NPM_PACKAGE_PROXY_REGISTRY:
        required: true
      NPM_PACKAGE_REGISTRY_TOKEN:
        required: false
      SLACK_WEBHOOK_URL:
        required: false
      GOOGLE_CHAT_WEBHOOK_URL:
        required: false

permissions:
  contents: read

jobs:
  check-docs:
    runs-on: ubuntu-latest
    outputs:
      node-version: ${{ steps.resolve-node.outputs.version }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: coroboros/ci/.github/actions/check-docs@v0
      - id: resolve-node
        uses: coroboros/ci/.github/actions/resolve-node-version@v0
        with:
          node-version: ${{ inputs.node-version }}

  setup-npmrc:
    runs-on: ubuntu-latest
    needs: [check-docs]
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: coroboros/ci/.github/actions/setup-npmrc@v0
        with:
          npm-config-file: ${{ secrets.NPM_CONFIG_FILE }}
          npm-extra-config: ${{ inputs.npm-extra-config }}

  check-npm-lint:
    runs-on: ubuntu-latest
    needs: [check-docs, setup-npmrc]
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: coroboros/ci/.github/actions/pnpm-install@v0
        with:
          node-version: ${{ needs.check-docs.outputs.node-version }}
      - run: pnpm run lint
        shell: bash

  build-js:
    runs-on: ubuntu-latest
    needs: [check-docs, setup-npmrc]
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: coroboros/ci/.github/actions/build-js@v0
        with:
          node-version: ${{ needs.check-docs.outputs.node-version }}

  build-version:
    runs-on: ubuntu-latest
    needs: [check-docs, setup-npmrc]
    if: |
      github.ref_type == 'tag'
      || contains(github.event.head_commit.message, 'Merge branch ''release/')
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: coroboros/ci/.github/actions/build-version@v0
        with:
          release-branch-pattern: ${{ inputs.release-branch-pattern }}
          release-source-commit-pattern: ${{ inputs.release-source-commit-pattern }}
          node-version: ${{ needs.check-docs.outputs.node-version }}

  test-npm-unit:
    runs-on: ubuntu-latest
    needs: [check-docs, build-js]
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: coroboros/ci/.github/actions/pnpm-install@v0
        with:
          node-version: ${{ needs.check-docs.outputs.node-version }}
      - run: pnpm test
        shell: bash

  deploy-package:
    runs-on: ubuntu-latest
    needs: [check-docs, build-js, build-version, test-npm-unit]
    if: ${{ github.ref_type == 'tag' }}
    permissions:
      id-token: write   # required for npm OIDC Trusted Publisher.
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: ${{ needs.check-docs.outputs.node-version }}
          registry-url: https://registry.npmjs.org

      - name: Enable corepack + activate pnpm
        shell: bash
        run: |
          corepack enable
          pnpm --version

      - name: Download .npmrc
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        continue-on-error: true
        with:
          name: npmrc
          path: .

      - name: Download dist
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: dist
          path: dist

      - name: Download package-version
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: package-version
          path: .

      - name: Install (prod, frozen lockfile, ignore scripts)
        # pnpm --frozen-lockfile fails if pnpm-lock.yaml is missing or stale,
        # giving us the integrity check that `npm audit signatures` provided.
        run: pnpm install --frozen-lockfile --ignore-scripts --prod
        shell: bash

      - name: pnpm publish (OIDC + NPM_PACKAGE_REGISTRY_TOKEN fallback)
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_PACKAGE_REGISTRY_TOKEN }}
          WITH_PROVENANCE: ${{ inputs.provenance }}
        run: |
          if [ "${WITH_PROVENANCE}" = "true" ]; then
            pnpm publish --provenance --no-git-checks
          else
            pnpm publish --no-git-checks
          fi
        shell: bash

  notify:
    needs: [deploy-package]
    if: always() && needs.deploy-package.result != 'skipped'
    uses: coroboros/ci/.github/workflows/notify.yml@v0
    with:
      release-branch-pattern: ${{ inputs.release-branch-pattern }}
      release-source-commit-pattern: ${{ inputs.release-source-commit-pattern }}
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      GOOGLE_CHAT_WEBHOOK_URL: ${{ secrets.GOOGLE_CHAT_WEBHOOK_URL }}
