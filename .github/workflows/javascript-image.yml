# Pipeline: check -> setup-npmrc -> lint -> build-js -> build-version (rules) -> test ->
#           build-image-name -> publish-image (needs: build-version?, build-image-name, setup-npmrc) -> notify.
# `publish-image` depends on `setup-npmrc` to embed the .npmrc file in the Docker build context
# so the install inside the image can authenticate against the private registry.
name: JavaScript Image CI

on:
  workflow_call:
    inputs:
      node-version:
        type: string
        required: false
        default: "22"
      dockerfile-path:
        type: string
        required: false
        default: Dockerfile
      image-target:
        type: string
        required: false
        default: ""
      image-tag:
        type: string
        required: false
        default: ""
      image-build-args:
        type: string
        required: false
        default: ""
      image-registry-mirror:
        type: string
        required: false
        default: ""
      release-branch-pattern:
        type: string
        required: false
        default: '^release[/]((0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*))$'
      release-source-commit-pattern:
        type: string
        required: false
        default: "^Merge branch 'release[/]((0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*))' into 'master'"
      npm-extra-config:
        type: string
        required: false
        default: ""
    secrets:
      NPM_CONFIG_FILE:
        required: true
      NPM_PACKAGE_REGISTRY:
        required: true
      NPM_PACKAGE_PROXY_REGISTRY:
        required: true
      NPM_PACKAGE_REGISTRY_TOKEN:
        required: true
      IMAGE_REGISTRY:
        required: true
      IMAGE_REGISTRY_USER:
        required: true
      IMAGE_REGISTRY_PASSWORD:
        required: true
      IMAGE_CACHE_REPO:
        required: false
      SLACK_WEBHOOK_URL:
        required: false
      GOOGLE_CHAT_WEBHOOK_URL:
        required: false

permissions:
  contents: read

jobs:
  check-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: coroboros/ci/.github/actions/check-readme@v0

  setup-npmrc:
    runs-on: ubuntu-latest
    needs: [check-readme]
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: coroboros/ci/.github/actions/setup-npmrc@v0
        with:
          npm-config-file: ${{ secrets.NPM_CONFIG_FILE }}
          npm-extra-config: ${{ inputs.npm-extra-config }}

  check-npm-lint:
    runs-on: ubuntu-latest
    needs: [setup-npmrc]
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: coroboros/ci/.github/actions/pnpm-install@v0
        with:
          node-version: ${{ inputs.node-version }}
      - run: pnpm run lint
        shell: bash

  build-js:
    runs-on: ubuntu-latest
    needs: [setup-npmrc]
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: coroboros/ci/.github/actions/build-js@v0
        with:
          node-version: ${{ inputs.node-version }}

  build-version:
    runs-on: ubuntu-latest
    needs: [setup-npmrc]
    if: |
      github.ref_type == 'tag'
      || contains(github.event.head_commit.message, 'Merge branch ''release/')
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: coroboros/ci/.github/actions/build-version@v0
        with:
          release-branch-pattern: ${{ inputs.release-branch-pattern }}
          release-source-commit-pattern: ${{ inputs.release-source-commit-pattern }}

  test-npm-unit:
    runs-on: ubuntu-latest
    needs: [build-js]
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: coroboros/ci/.github/actions/pnpm-install@v0
        with:
          node-version: ${{ inputs.node-version }}
      - run: pnpm test
        shell: bash

  build-image-name:
    runs-on: ubuntu-latest
    needs: [test-npm-unit]
    outputs:
      image: ${{ steps.name.outputs.image }}
      image-name: ${{ steps.name.outputs.image-name }}
      image-tag: ${{ steps.name.outputs.image-tag }}
      image-ref-tag: ${{ steps.name.outputs.image-ref-tag }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - id: name
        uses: coroboros/ci/.github/actions/build-image-name@v0
        with:
          image-registry: ${{ secrets.IMAGE_REGISTRY }}
          image-target: ${{ inputs.image-target }}
          image-tag: ${{ inputs.image-tag }}

  publish-image:
    # Source override: needs build-version (optional), build-image-name (artifacts), setup-npmrc (artifacts).
    needs: [build-image-name, setup-npmrc, build-version]
    # `build-version` is optional in the source — translate via `always()` + explicit success/skipped check.
    if: |
      always()
      && needs.build-image-name.result == 'success'
      && needs.setup-npmrc.result == 'success'
      && (needs.build-version.result == 'success' || needs.build-version.result == 'skipped')
    runs-on: ubuntu-latest
    container:
      image: gcr.io/kaniko-project/executor:debug
      options: --user 0
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        continue-on-error: true
        with:
          name: npmrc
          path: .
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        continue-on-error: true
        with:
          name: package-version
          path: .
      - uses: coroboros/ci/.github/actions/publish-image@v0
        with:
          dockerfile-path: ${{ inputs.dockerfile-path }}
          image-build-args: ${{ inputs.image-build-args }}
          image-cache-repo: ${{ secrets.IMAGE_CACHE_REPO }}
          image-registry: ${{ secrets.IMAGE_REGISTRY }}
          image-registry-user: ${{ secrets.IMAGE_REGISTRY_USER }}
          image-registry-password: ${{ secrets.IMAGE_REGISTRY_PASSWORD }}
          image-registry-mirror: ${{ inputs.image-registry-mirror }}
          image: ${{ needs.build-image-name.outputs.image }}
          image-name: ${{ needs.build-image-name.outputs.image-name }}
          image-ref-tag: ${{ needs.build-image-name.outputs.image-ref-tag }}

  notify:
    needs: [publish-image]
    if: always() && needs.publish-image.result != 'skipped'
    uses: coroboros/ci/.github/workflows/notify.yml@v0
    with:
      release-branch-pattern: ${{ inputs.release-branch-pattern }}
      release-source-commit-pattern: ${{ inputs.release-source-commit-pattern }}
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      GOOGLE_CHAT_WEBHOOK_URL: ${{ secrets.GOOGLE_CHAT_WEBHOOK_URL }}
